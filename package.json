{
  "name": "publish-nuget",
  "version": "1.0.0",
  "description": "GitHub action to build, pack & publish nuget packages automatically when a project version is updated",
  "main": "index.js",
  "dependencies": {

  },
  "devDependencies": {
    "@actions/core": "^1.2.6",
    "@vercel/ncc": "^0.28.2"},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build index.js"
  },
  "author": "",
  "license": "ISC"
}
